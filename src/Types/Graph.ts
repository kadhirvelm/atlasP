export interface IScoreMainPerson {
  isMain: boolean;
}

export interface IScore {
  isMain: boolean;
  eventScore: number;
  genderScore: number;
  ageScore: number;
  likeScore: number;
  dislikeScore: number;
  finalScore: number;
}
